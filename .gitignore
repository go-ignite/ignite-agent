# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
.idea

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
cmd/ignite-agent/ignite-agent
/ignite-agent
vendor
/agent-cli
/agent-rpc
.DS_Store
/env
